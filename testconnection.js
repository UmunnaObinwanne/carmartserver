/*

import database.from '../'; // Adjust the path

async function testConnection() {
  try {
    await mongoose.connection.db.admin().ping();
    console.log("Successfully connected to MongoDB");
  } catch (error) {
    console.error("Failed to connect to MongoDB", error);
  }
}

testConnection();

*/
